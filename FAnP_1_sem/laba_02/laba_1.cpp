//#include <iostream>
//int main()
//{
//	double r, y, a = 1.75, b = 4.5e-5;
//	y = a*exp(-2*b) - a * sqrt(1+a);
//	r = log(1 + 20*b) / (1+a);
//	std::cout << "y=" << y;
//	std::cout << " r=" << r;
//}
//#include <iostream>
//int main()
//{
//	int k = 4;
//	double t, u, a = 8.1, x = 2e-4;
//	t = 2 * k / a + log(2 + x);
//	u = sqrt(k - 1) / (t + 1);
//	std::cout << "t=" << t;
//	std::cout << " u=" << u;
//}
//#include <iostream>
//int main()
//{
//	int b = 40;
//	double v, w, x = 1.1, a = 5e-6;
//	w = (a+b)*tan(x)/(x+1);
//	v = 1/2*b-sqrt(w-a*b);
//	std::cout << "w=" << w;
//	std::cout << " v=" << v;
//}
#include <iostream>
int main()
{
	/*int j = 12;
	double t, s, x = 0.1, y = 5e-6;
	s = 0.4*x-1/j*tan(y);
	t = s-sin(s);
	std::cout << "s=" << s;
	std::cout << " t=" << t;*/
    int b = 40;
	double s, w, v, a = 1, 5; b = -8,1, j=4, t = 4*10^(-8),
	s = sqrt(t * a / (t + 1)) + 4 * exp(2 * b);
	w = s*a/(1+0,1*a);
	v = s + j*sqrt(a2 + b^2);
	std::cout << "s=" << s;
	std::cout << "w=" << w;
	std::cout << "v=" << v;

}